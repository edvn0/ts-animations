{
  "name": "ts-animations",
  "private": true,
  "packageManager": "pnpm@10.7.0",
  "scripts": {
    "build": "pnpm -r build",
    "start:dev": "concurrently \"pnpm --filter client dev\" \"pnpm --filter server start:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
